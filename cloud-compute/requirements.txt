# No external Python packages. The worker uses only the standard library plus the
# Cloudflare `workers` runtime module, so it runs under plain `wrangler dev` without
# the beta pywrangler package-vendoring toolchain. (numpy/FastAPI were dropped: a
# 5-feature dot product and manual routing don't need them.)
